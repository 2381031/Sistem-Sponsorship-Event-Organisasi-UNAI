{
  "installCommand": "npm install && cd backend && npm install",
  "buildCommand": "npm run --prefix backend build && npx esbuild backend/dist/main.js --bundle --platform=node --format=cjs --outfile=api/_backend.js --minify --external:@nestjs/websockets --external:@nestjs/microservices && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "functions": {
    "api/index.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index.js"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
